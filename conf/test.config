/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running minimal tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a fast and simple pipeline test.

    Use as follows:
        nextflow run daisybio/domainbenchmark -profile test,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

process {
    resourceLimits = [
        cpus: 4,
        memory: '15.GB',
        time: '1.h'
    ]
}

params {
    config_profile_name        = 'Test profile'
    config_profile_description = 'Minimal in-repo fixture used for smoke tests / CI.'

    // I/O
    input                         = "${projectDir}/assets/samplesheet_test.csv"
    outdir                        = './results-test'

    // Pipeline behaviour: run only the cheapest feature + skip every graph model.
    machine_learning_features     = 'aacomp'
    large_features                = ''
    max_machine_learning_features = 1
    graph_models                  = ''
    skip                          = ''

    // Boilerplate
    seed                          = 42
}
