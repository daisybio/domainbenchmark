{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/daisybio/cobinet-benchmarking-pipeline/main/assets/schema_input.json",
    "title": "daisybio/cobinet-benchmarking-pipeline pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "id": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Database id must be a non-empty string with no spaces.",
                "meta": ["id"]
            },
            "db_path": {
                "type": "string",
                "format": "directory-path",
                "exists": true,
                "pattern": "^\\S+$",
                "errorMessage": "Database path must be a directory containing train/test/optimization sqlite splits."
            }
        },
        "required": ["id", "db_path"]
    }
}
